# Action — coding

Use this mode for **implementation and refactors** (not primary review — use `reviewing.md`).

MUST: Implement what was asked; no drive-by features or scope creep.
MUST: Prefer small, reviewable steps; keep diffs focused.
MUST: Add or update automated checks when behavior changes (tests, types, linters — whatever this repo uses); if not feasible, state why in the summary.
MUST: After edits, run or reason about the repo’s standard quality gates when available.

When `runtime/plan.md` exists, align steps with the plan unless the user changes scope.
