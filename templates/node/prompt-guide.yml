version: "1"

project:
  name: my-app
  type: node
  description: Node.js project

tools:
  claude_code: true
  codex: false
  cursor: true

context:
  tech_stack:
    - Node.js
    - TypeScript

  summary: |
    Node.js + TypeScript. ESM modules. strict mode.

  path_rules:
    - name: typescript
      path: ["**/*.{ts,tsx}"]
      content: |
        - strict TypeScript
        - Prefer async/await
        - Result pattern or explicit error handling

  skills: []
  agents: []
  forbidden:
    - "any type"
    - "Committing console.log"

  cursor:
    always_apply_rules: [core]
