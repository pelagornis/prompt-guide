version: "1"

project:
  name: my-app
  type: web-vue
  description: Vue 3 web app

tools:
  claude_code: true
  codex: false
  cursor: true

context:
  tech_stack:
    - Vue 3
    - TypeScript
    - Vite

  summary: |
    Vue 3 Composition API + script setup.
    Pinia for state. Tailwind CSS.

  path_rules:
    - name: vue-sfc
      path: ["**/*.vue"]
      content: |
        - script setup + TypeScript
        - Use defineProps / defineEmits
        - Extract logic into composables

  skills: []
  agents: []
  forbidden:
    - "New Options API usage"

  cursor:
    always_apply_rules: [core]
